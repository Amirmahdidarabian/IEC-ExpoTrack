# B Nazanin font asset

Persian exhibition dates are configured to use `BNazanin.woff2` from this directory.
The font file is not committed because a redistributable licensed copy was not supplied.
Place your licensed webfont at `public/fonts/BNazanin.woff2`; the UI falls back to Tahoma until it is present.
